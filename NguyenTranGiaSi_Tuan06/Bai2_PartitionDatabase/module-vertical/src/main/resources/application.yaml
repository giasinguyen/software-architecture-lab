server:
  port: 8082

spring:
  application:
    name: module-vertical
  datasource:
    url: jdbc:mariadb://localhost:3306/vertical_db
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
